
services:
  postgres:
    image: postgres:15-alpine
    container_name: yato-postgres
    restart: always
    environment:
      POSTGRES_USER: hermes
      POSTGRES_PASSWORD: hermespass
      POSTGRES_DB: hermesops
    ports:
      - "5440:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: yato-redis
    restart: always
    ports:
      - "6380:6379"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: yato-backend
    restart: always
    environment:
      DATABASE_URL: "postgresql://hermes:hermespass@postgres:5432/hermesops?schema=public"
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY}
    depends_on:
      - postgres
      - redis
    env_file:
      - .env
    volumes:
      - .env:/app/.env
    ports:
      - "4000:3000"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: yato-frontend
    restart: always
    environment:
      NEXT_PUBLIC_API_URL: ${API_URL:-http://localhost:4000}
    depends_on:
      - backend
    ports:
      - "4001:3000"

  nginx:
    image: nginx:alpine
    container_name: yato-nginx
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "9090:80"
      - "9443:443"
    depends_on:
      - frontend
      - backend

volumes:
  postgres_data:
